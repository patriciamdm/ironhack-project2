<div class="col-md-6">
    <a href="/movies/{{list.db_id}}">
        {{#if list.poster_path}}
        <img src='https://image.tmdb.org/t/p/w300{{list.poster_path}}' alt="{{list.title}}">
        {{/if}}

        <h6>{{list.title}}</h6>
    </a>
</div>